# 🏗️ Frontend Architecture Diagram

## 📊 Visual Overview

```
┌─────────────────────────────────────────────────────────────┐
│                         BROWSER                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                    index.html                          │  │
│  │              <div id="root"></div>                     │  │
│  └───────────────────┬───────────────────────────────────┘  │
│                      │                                       │
│                      ↓                                       │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                   main.tsx                             │  │
│  │        ReactDOM.render(<App />)                       │  │
│  └───────────────────┬───────────────────────────────────┘  │
│                      │                                       │
│                      ↓                                       │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                   App.tsx                              │  │
│  │   ┌──────────────────────────────────────┐            │  │
│  │   │  <Routes>                             │            │  │
│  │   │    / → Landing Page                   │            │  │
│  │   │    /app → Home (Workflow Builder)    │            │  │
│  │   └──────────────────────────────────────┘            │  │
│  └─────────────┬──────────────────┬─────────────────────┘  │
│                │                  │                         │
│                ↓                  ↓                         │
│       ┌────────────────┐  ┌──────────────────┐            │
│       │  Landing.tsx   │  │    Home.tsx      │            │
│       │  (Welcome)     │  │  (Main Builder)  │            │
│       └────────────────┘  └──────┬───────────┘            │
│                                   │                         │
│                                   ↓                         │
│                    ┌──────────────────────────┐            │
│                    │   Home.tsx Structure:    │            │
│                    │                          │            │
│                    │   ┌────────────────┐    │            │
│                    │   │   Sidebar      │    │            │
│                    │   │  (Drag nodes)  │    │            │
│                    │   └────────────────┘    │            │
│                    │   ┌────────────────┐    │            │
│                    │   │   ReactFlow    │    │            │
│                    │   │   (Canvas)     │    │            │
│                    │   │   - CustomNode │    │            │
│                    │   │   - Edges      │    │            │
│                    │   └────────────────┘    │            │
│                    │   ┌────────────────┐    │            │
│                    │   │ ConfigPanel    │    │            │
│                    │   │  (Settings)    │    │            │
│                    │   └────────────────┘    │            │
│                    └──────────────────────────┘            │
└───────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

```
USER ACTIONS
    ↓
┌───────────────────────────────────────────────┐
│              EVENT HANDLERS                    │
│  • onDrop (drag node to canvas)              │
│  • onConnect (draw line)                      │
│  • onNodeDoubleClick (open config)           │
│  • executeWorkflow (run pipeline)            │
└───────────┬───────────────────────────────────┘
            ↓
┌───────────────────────────────────────────────┐
│           ZUSTAND STORE (store.ts)            │
│  ┌─────────────────────────────────────────┐ │
│  │  State:                                  │ │
│  │    • nodes: []                          │ │
│  │    • edges: []                          │ │
│  │                                          │ │
│  │  Actions:                               │ │
│  │    • addNode()                          │ │
│  │    • updateNode()                       │ │
│  │    • deleteNode()                       │ │
│  │    • addEdge()                          │ │
│  │    • deleteEdge()                       │ │
│  └─────────────────────────────────────────┘ │
└───────────┬───────────────────────────────────┘
            ↓
┌───────────────────────────────────────────────┐
│              COMPONENTS RE-RENDER              │
│  • Home.tsx updates                           │
│  • CustomNode.tsx updates                     │
│  • ReactFlow updates                          │
└───────────┬───────────────────────────────────┘
            ↓
┌───────────────────────────────────────────────┐
│                 UI UPDATES                     │
│  User sees the changes on screen              │
└───────────────────────────────────────────────┘
```

---

## 🎯 Component Hierarchy

```
App
 ├── Router
 │    ├── Route (path="/")
 │    │    └── Landing
 │    │         ├── Slides
 │    │         ├── Navigation
 │    │         └── "Get Started" Button
 │    │
 │    └── Route (path="/app")
 │         └── Home ⭐ MAIN COMPONENT
 │              ├── Sidebar
 │              │    ├── Node Buttons (drag)
 │              │    └── Execute Button
 │              │
 │              ├── ReactFlow (Canvas)
 │              │    ├── CustomNode (multiple)
 │              │    │    ├── Header (icon, title)
 │              │    │    ├── Body (description)
 │              │    │    ├── Results Display
 │              │    │    ├── Input Handle (left)
 │              │    │    └── Output Handle (right)
 │              │    │
 │              │    ├── Edges (lines)
 │              │    ├── Background (grid)
 │              │    ├── Controls (zoom/pan)
 │              │    ├── MiniMap
 │              │    └── Panel (node count)
 │              │
 │              └── NodeConfigPanel (conditional)
 │                   ├── Form Fields
 │                   └── Save Button
 │
 └── Global State (Zustand)
      • Accessible by all components
```

---

## 🗂️ File Dependencies

```
main.tsx
  └─→ App.tsx
       ├─→ pages/Landing.tsx
       │    └─→ components/ui/* (Button, etc.)
       │
       └─→ pages/Home.tsx ⭐
            ├─→ components/Sidebar.tsx
            │    ├─→ lib/node-definitions.ts
            │    └─→ components/ui/Button.tsx
            │
            ├─→ components/CustomNode.tsx
            │    ├─→ lib/node-definitions.ts
            │    ├─→ lib/types.ts
            │    └─→ lucide-react (icons)
            │
            ├─→ components/NodeConfigPanel.tsx
            │    ├─→ lib/store.ts
            │    ├─→ lib/types.ts
            │    └─→ components/ui/* (Input, Select, etc.)
            │
            ├─→ lib/store.ts (Global State)
            │    └─→ lib/types.ts
            │
            ├─→ lib/executor.ts
            │    ├─→ api/client.ts
            │    ├─→ lib/node-definitions.ts
            │    └─→ lib/types.ts
            │
            └─→ reactflow (Library)
```

---

## 🔄 Workflow Execution Flow

```
USER CLICKS "EXECUTE WORKFLOW"
    ↓
┌─────────────────────────────────────────┐
│  1. Find Trigger Nodes                  │
│     (nodes with no incoming edges)      │
└─────────────┬───────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  2. For Each Trigger Node:              │
│     executeNodeChain(nodeId, null)      │
└─────────────┬───────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  3. Execute Node:                       │
│     • Mark as "executing"               │
│     • Call executor.executeNode()       │
│     • Send request to Backend API       │
└─────────────┬───────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  4. Backend Processes:                  │
│     • Upload: Save file                 │
│     • Clean: Remove missing values      │
│     • Train: Train ML model             │
│     • etc.                              │
└─────────────┬───────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  5. Receive Result:                     │
│     { success: true, output: {...} }    │
└─────────────┬───────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  6. Update Node:                        │
│     • Save output                       │
│     • Show success/error                │
│     • Mark as "done"                    │
└─────────────┬───────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  7. Find Connected Nodes:               │
│     edges.filter(e => e.source == id)   │
└─────────────┬───────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  8. Execute Each Child Node:            │
│     executeNodeChain(childId, output)   │
└─────────────┬───────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  9. Repeat Steps 3-8 Recursively        │
│     Until All Nodes Done                │
└─────────────┬───────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  10. Workflow Complete!                 │
│      Show Final Results                 │
└─────────────────────────────────────────┘
```

---

## 🎨 Node Structure Diagram

```
┌──────────────────────────────────────────────┐
│  CustomNode Component                        │
│                                              │
│  ○ Input Handle (left side)                 │
│  │                                           │
│  ├─────────────────────────────────────────┐ │
│  │  HEADER (colored)                       │ │
│  │  ┌────┐                                 │ │
│  │  │Icon│ Label          [Status]         │ │
│  │  └────┘                                 │ │
│  └─────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────┐ │
│  │  BODY (white)                           │ │
│  │                                         │ │
│  │  Description text...                   │ │
│  │                                         │ │
│  │  ┌───────────────────────────────────┐ │ │
│  │  │ Results Display                   │ │ │
│  │  │ ✓ Dataset loaded: 100 rows        │ │ │
│  │  └───────────────────────────────────┘ │ │
│  │                                         │ │
│  │  ┌───────────────────────────────────┐ │ │
│  │  │ Error Display (if error)          │ │ │
│  │  │ ❌ Missing required field         │ │ │
│  │  └───────────────────────────────────┘ │ │
│  └─────────────────────────────────────────┘ │
│  │                                           │
│  ○ Output Handle (right side)                │
└──────────────────────────────────────────────┘

Status Icons:
  ⟳ Spinning loader (executing)
  ✓ Check mark (success)
  ⚠ Warning (error)
```

---

## 🗄️ State Management Pattern

```
┌─────────────────────────────────────────────┐
│           ZUSTAND STORE                      │
│                                              │
│  create((set, get) => ({                    │
│                                              │
│    STATE:                                   │
│    ├─ nodes: []                             │
│    └─ edges: []                             │
│                                              │
│    ACTIONS:                                 │
│    ├─ addNode: (node) => {                  │
│    │    set({ nodes: [...nodes, node] })    │
│    │  }                                      │
│    │                                         │
│    ├─ updateNode: (id, data) => {           │
│    │    set({                                │
│    │      nodes: nodes.map(n =>             │
│    │        n.id === id                     │
│    │          ? { ...n, data: {...} }       │
│    │          : n                           │
│    │      )                                  │
│    │    })                                   │
│    │  }                                      │
│    │                                         │
│    └─ deleteNode: (id) => { ... }           │
│                                              │
│  }))                                         │
└─────────────────────────────────────────────┘

USAGE IN COMPONENT:
┌─────────────────────────────────────────────┐
│  function Home() {                           │
│    // Subscribe to store                    │
│    const { nodes, addNode } =               │
│      useWorkflowStore();                    │
│                                              │
│    // Use the data                          │
│    console.log(nodes);                      │
│                                              │
│    // Modify the data                       │
│    addNode(newNode);                        │
│  }                                           │
└─────────────────────────────────────────────┘
```

---

## 🎭 React Hooks Flow

```
COMPONENT LIFECYCLE:

1. MOUNT (Component appears)
   ↓
   useEffect(() => {
     // Runs once when component mounts
     console.log('Hello!');
   }, []);  // Empty array = run once

2. UPDATE (State/props change)
   ↓
   useEffect(() => {
     // Runs when 'nodes' changes
     console.log('Nodes changed:', nodes);
   }, [nodes]);  // Dependency array

3. UNMOUNT (Component removed)
   ↓
   useEffect(() => {
     return () => {
       // Cleanup function
       console.log('Goodbye!');
     };
   }, []);


useState FLOW:
┌─────────────────────────────────────────┐
│  const [count, setCount] = useState(0); │
│                                         │
│  Initial: count = 0                     │
│  User clicks button                     │
│  Call: setCount(1)                      │
│  React re-renders component             │
│  New: count = 1                         │
└─────────────────────────────────────────┘


useCallback FLOW:
┌─────────────────────────────────────────┐
│  const handleClick = useCallback(() => { │
│    console.log(nodes);                  │
│  }, [nodes]);                           │
│                                         │
│  • Function created once               │
│  • Only recreated if 'nodes' changes   │
│  • Prevents unnecessary re-renders     │
└─────────────────────────────────────────┘
```

---

## 📡 API Communication Flow

```
FRONTEND                     BACKEND
   │                            │
   │  1. User uploads CSV       │
   │  ──────────────────────→   │
   │  POST /api/ml/upload       │
   │  { file: File }            │
   │                            │
   │                        2. Save file
   │                        3. Load data
   │                        4. Generate ID
   │                            │
   │  5. Return pipeline_id     │
   │  ←──────────────────────   │
   │  { pipeline_id: "abc123",  │
   │    dataset_info: {...} }   │
   │                            │
   │  6. Store in node output   │
   │                            │
   │  7. User cleans data       │
   │  ──────────────────────→   │
   │  POST /api/ml/clean        │
   │  { pipeline_id: "abc123",  │
   │    strategy: "drop_rows" } │
   │                            │
   │                        8. Clean data
   │                            │
   │  9. Return result          │
   │  ←──────────────────────   │
   │  { cleaned: true }         │
   │                            │
   │  ... continues for each    │
   │      node in workflow      │
```

---

## 🎨 Styling Architecture

```
TAILWIND CSS CLASSES:
┌─────────────────────────────────────────┐
│  Utility-First Approach                 │
│                                         │
│  <div className="                       │
│    flex              ← Layout           │
│    items-center      ← Alignment        │
│    gap-4             ← Spacing          │
│    p-4               ← Padding          │
│    bg-blue-500       ← Background       │
│    text-white        ← Text Color       │
│    rounded-lg        ← Border Radius    │
│    shadow-md         ← Shadow           │
│    hover:bg-blue-600 ← Hover State      │
│    dark:bg-gray-800  ← Dark Mode        │
│  ">                                     │
│    Content                              │
│  </div>                                 │
└─────────────────────────────────────────┘

COLOR SYSTEM:
  • bg-purple-500 → Upload nodes
  • bg-pink-500   → Clean nodes
  • bg-indigo-500 → Preprocess nodes
  • bg-cyan-500   → Split nodes
  • bg-emerald-500→ Train nodes
  • bg-violet-500 → Results nodes
```

---

## 🚀 This diagram helps you understand:
1. How files connect to each other
2. How data flows through the app
3. How components are structured
4. How workflow execution works
5. How state management works

**Pro Tip**: Print this out and refer to it while coding!
